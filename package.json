{
	"name": "orc-shared",
	"version": "0.8.6-pre.1",
	"description": "Shared code for Orckestra applications",
	"main": "dist/",
	"scripts": {
		"build": "orc-scripts prep && orc-scripts build --ignore \"**/*.test.js\",\"src/translations/*\" --no-copy-files",
		"build:clean": "npm run clean && npm run build",
		"clean": "orc-scripts clean",
		"watch": "npm run build -- --watch",
		"test": "orc-scripts test",
		"coverage": "orc-scripts test --coverage",
		"coveralls": "cat coverage/lcov.info | coveralls",
		"prepublishOnly": "npm run build:clean"
	},
	"files": [
		"dist"
	],
	"repository": {
		"type": "git",
		"url": "git+https://github.com/Orckestra/orc-shared.git"
	},
	"author": "Gert Sønderby <gert.sonderby@orckestra.com>",
	"license": "MIT",
	"bugs": {
		"url": "https://github.com/Orckestra/orc-shared/issues"
	},
	"homepage": "https://github.com/Orckestra/orc-shared#readme",
	"devDependencies": {
		"orc-scripts": "0.7.3-pre.3"
	},
	"peerDependencies": {
		"orc-scripts": "^0.7.2"
	},
	"sideEffects": false,
	"lint-staged": {
		"*.{js,json,md}": [
			"prettier --write",
			"git add"
		],
		"*.js": [
			"eslint"
		]
	},
	"husky": {
		"hooks": {
			"pre-commit": "lint-staged"
		}
	},
	"dependencies": {}
}
